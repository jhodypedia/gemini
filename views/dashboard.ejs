<div class="row gy-4">
  <div class="col-lg-4">
    <div class="card p-4">
      <h5 class="text-ocean">1) Gemini API Key</h5>
      <p class="small text-muted">Masukkan API Key Gemini (disimpan di session server).</p>
      <form id="formKey">
        <input name="apiKey" class="form-control mb-2" placeholder="Masukkan API Key...">
        <button class="btn btn-ocean w-100">Simpan API Key</button>
      </form>

      <hr>

      <h6 class="text-ocean">Upload Assets</h6>
      <form id="formUpload" enctype="multipart/form-data">
        <div class="mb-2">
          <label class="form-label">Images (banyak)</label>
          <input type="file" name="images" multiple accept="image/*" class="form-control">
        </div>
        <div class="mb-2">
          <label class="form-label">Audio (opsional)</label>
          <input type="file" name="audio" accept="audio/*" class="form-control">
        </div>
        <button class="btn btn-outline-ocean w-100">Upload</button>
      </form>
      <div id="uploadPreview" class="mt-3"></div>
    </div>
  </div>

  <div class="col-lg-8">
    <div class="card p-4">
      <h5 class="text-ocean">2) Generate Videos (VEO)</h5>
      <form id="formGenerate" enctype="multipart/form-data">
        <div class="mb-3">
          <label class="form-label">Prompt / Topik</label>
          <input name="prompt" class="form-control" placeholder="Mis: 5 tips produktif kerja dari rumah">
        </div>

        <div class="row g-2 mb-3">
          <div class="col-md-4">
            <label class="form-label">Preset</label>
            <select name="preset" class="form-select">
              <option value="reel">Reel / Shorts</option>
              <option value="landscape">Landscape</option>
              <option value="square">Square</option>
            </select>
          </div>
          <div class="col-md-4">
            <label class="form-label">Rasio</label>
            <select name="ratio" class="form-select">
              <option value="9:16">9:16</option>
              <option value="16:9">16:9</option>
              <option value="1:1">1:1</option>
            </select>
          </div>
          <div class="col-md-4">
            <label class="form-label">Total Video</label>
            <input name="total" type="number" class="form-control" value="10" min="1" max="50">
          </div>
        </div>

        <div class="row g-2 mb-3">
          <div class="col-md-4">
            <label class="form-label">Duration (s)</label>
            <input name="duration" type="number" class="form-control" value="8" min="4" max="60">
          </div>
          <div class="col-md-4">
            <label class="form-label">Ken Burns</label>
            <select name="kenburns" class="form-select"><option value="true" selected>On</option><option value="false">Off</option></select>
          </div>
          <div class="col-md-4">
            <label class="form-label">Fade</label>
            <select name="fade" class="form-select"><option value="true" selected>On</option><option value="false">Off</option></select>
          </div>
        </div>

        <div class="row g-2 mb-3">
          <div class="col-md-6">
            <label class="form-label">Font (server fonts folder)</label>
            <input name="fontName" class="form-control" placeholder="e.g. Poppins">
          </div>
          <div class="col-md-6">
            <label class="form-label">Target Words (script)</label>
            <input name="wordsTarget" class="form-control" value="60" type="number" min="20" max="200">
          </div>
        </div>

        <input type="hidden" name="imagesJson" id="imagesJson">
        <input type="hidden" name="audioUrl" id="audioUrl">

        <div class="d-grid gap-2">
          <button class="btn btn-ocean">🚀 Generate</button>
          <button type="button" id="btnLatest" class="btn btn-outline-ocean">Refresh Latest</button>
        </div>
      </form>

      <hr>
      <div id="jobs"></div>
      <div id="results" class="mt-3 d-flex flex-wrap gap-3"></div>
    </div>
  </div>
</div>
