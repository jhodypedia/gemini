<% include('layouts/main') %>

<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card p-4 halo">
      <h4>🔑 Set Gemini API Key</h4>
      <p class="small text-muted">Simpan API key kamu agar backend bisa memanggil Gemini Veo 3 untuk menghasilkan naskah/video. Hanya kamu yang dapat melihat key ini di akunmu.</p>

      <% if (typeof user !== 'undefined' && user) { %>
        <form method="post" action="/set-apikey">
          <div class="mb-2">
            <input type="text" name="apiKey" value="<%= user.geminiApiKey || '' %>" class="form-control" placeholder="Masukkan Gemini API Key">
          </div>
          <div class="d-flex gap-2">
            <button class="btn-ocean">Simpan</button>
            <a href="/generate-video" class="btn btn-outline">Kembali</a>
          </div>
        </form>
      <% } else { %>
        <div class="alert alert-warning">User tidak ditemukan. Silakan login ulang.</div>
      <% } %>
    </div>
  </div>
</div>
